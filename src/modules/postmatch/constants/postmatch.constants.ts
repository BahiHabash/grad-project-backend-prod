/** Maximum time to wait for the AI microservice response (ms). */
export const AI_CLIENT_TIMEOUT_MS = 20_000;

/** Maximum time to wait for a single LLM adapter call (ms). */
export const LLM_ADAPTER_TIMEOUT_MS = 15_000;

/** Default page size for listing reports. */
export const DEFAULT_PAGE_LIMIT = 10;

/** Maximum allowed page size. */
export const MAX_PAGE_LIMIT = 50;
